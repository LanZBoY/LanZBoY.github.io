---
import Layout from '../../layouts/Layout.astro';
import { notes, profile } from '../../data/site';

const meta = notes.find((n) => n.slug === 'eks-fluentbit-cloudwatch-log-groups')!;
---

<Layout title={`${meta.title} · ${profile.name}`}>
  <nav class="nav">
    <div class="container">
      <a href="/" class="logo">{profile.name}<span>.dev</span></a>
      <div class="links">
        <a href="/#about">關於</a>
        <a href="/#experience">經歷</a>
        <a href="/#projects">作品</a>
        <a href="/#awards">殊榮</a>
        <a href="/notes/" class="active">筆記</a>
        <a href="/#contact">聯絡</a>
      </div>
    </div>
  </nav>

  <article class="note">
    <div class="container">
      <a class="back-link" href="/notes/">← 回筆記列表</a>
      <p class="note-date">{meta.date}</p>
      <h1>{meta.title}</h1>
      <div class="tags">{meta.tags.map((t) => <span>{t}</span>)}</div>

      <div class="prose">
        <blockquote>
          <p><strong>寫在前面</strong> —— 我是個台中人，為了工作剛搬到台北。對台北的第一印象：人好多、好濕，
          但每天抬頭都看得到 101。雖然還有點不習慣，不過為了成長，也就豁出去了。</p>
          <p>這個系列就從入職的第一個任務開始記起，把一路上做過的事跟踩過的坑慢慢補上來。</p>
        </blockquote>
        <p>
          入職的第一個任務。背景：EKS 內部的 container log 透過 <strong>Fluent Bit</strong> 收集後送進
          <strong>AWS CloudWatch Logs</strong>。原本所有服務的 log 都進同一個 log group、全混在一起，
          一出錯就很難排查；改成<strong>按 app name 切分 log group</strong> 之後清楚很多。
          這篇順手把「Fluent Bit 到底怎麼跟 CloudWatch log group 串起來」也記下來。
        </p>

        <h2>1. 資料流：log 怎麼從 Pod 到 CloudWatch</h2>
        <div class="flow">
          <div class="flow-node"><strong>container log 檔</strong><small>node 上 /var/log/containers/*.log（CRI 格式）</small></div>
          <div class="flow-arrow"><span class="flow-label">DaemonSet：每個 node 一個 Fluent Bit pod</span></div>
          <div class="flow-node"><strong>tail input</strong><small>讀檔、打 tag</small></div>
          <div class="flow-arrow"><span class="flow-label">kubernetes filter</span><span class="flow-tip">補 namespace / pod / container / labels</span></div>
          <div class="flow-node"><strong>cloudwatch_logs output</strong><small>用 metadata 決定寫哪個 log group</small></div>
          <div class="flow-arrow"><span class="flow-label">IRSA 身份 → PutLogEvents</span></div>
          <div class="flow-node flow-accent"><strong>CloudWatch Logs</strong><small>per-app log group</small></div>
        </div>
        <p>幾個關鍵角色：</p>
        <ul>
          <li><strong>為什麼是 DaemonSet</strong>：log 檔散在每個 node 的本機路徑上，所以每個 node 都要有一個 collector。container runtime（containerd）會把每個 container 的 stdout/stderr 寫成 <code>/var/log/containers/&lt;pod&gt;_&lt;ns&gt;_&lt;container&gt;-&lt;id&gt;.log</code>，內容是 CRI 格式的一行行記錄。</li>
          <li><strong>kubernetes filter 是關鍵</strong>：tail 出來的原始 log 只知道「檔名」，要靠這個 filter 去查 API server / 用 tag 推導，把 <code>namespace_name</code>、<code>pod_name</code>、<code>container_name</code>、<code>labels</code>、<code>annotations</code> 灌進每一筆 record。<strong>之後要按 app 切 log group，全靠這步補進來的 metadata。</strong></li>
          <li><strong>log group 只是 output 的一個目的地字串</strong>：對 Fluent Bit 而言「串接 CloudWatch」就是 <code>cloudwatch_logs</code> output 拿 group 名稱去呼叫 CloudWatch API。<code>auto_create_group On</code> 時 group 不存在會自動建。</li>
          <li><strong>身份走 IRSA</strong>：Fluent Bit 的 ServiceAccount 綁一個 IAM role，policy 至少要有 <code>logs:CreateLogGroup</code>、<code>logs:CreateLogStream</code>、<code>logs:PutLogEvents</code>、<code>logs:DescribeLogStreams</code>（要設 retention 再加 <code>logs:PutRetentionPolicy</code>）。</li>
        </ul>

        <h2>2. 問題：全部混在同一個 log group</h2>
        <p>
          原本 output 寫死一個靜態 <code>log_group_name</code>，所有 namespace、所有 app 的 log 全部塞進同一個 group：
        </p>
        <pre><code>{`[OUTPUT]
    Name              cloudwatch_logs
    Match             kube.*
    region            us-west-2
    log_group_name    /eks/all          # ← 所有東西都進這一個
    log_stream_prefix from-fluent-bit-
    auto_create_group On`}</code></pre>
        <p>
          症狀：要查某個服務的錯誤，得在一坨混雜所有服務的 log 裡撈，
          stream 也分不出是誰，排查成本很高。log group 是 CloudWatch 裡權限、retention、metric filter、訂閱的最小單位，
          全部混在一起代表這些都沒辦法分服務管理。
        </p>

        <h2>3. 解法：用 log_group_template 動態切分（以 namespace 為 key）</h2>
        <p>
          目標是「一個 app 一個 log group」。我們的叢集是 <strong>一個 app 一個 namespace</strong>，
          所以實作上直接用 <strong>namespace 當切分 key</strong> 最省事 —— per-namespace 在這裡就等於 per-app，
          不必依賴每個 pod 都正確貼某個 <code>app</code> label。
        </p>
        <p>
          較新的 <code>cloudwatch_logs</code>（Go 版）output 支援 <code>log_group_template</code> /
          <code>log_stream_template</code>，可以用 <strong>record accessor</strong> 語法（<code>$key['nested']</code>）
          直接吃前面 kubernetes filter 補進來的 metadata，動態決定 log group：
        </p>
        <pre><code>{`[INPUT]
    Name              tail
    Tag               kube.*
    Path              /var/log/containers/*.log
    multiline.parser  cri              # containerd / CRI 格式
    Mem_Buf_Limit     50MB
    Skip_Long_Lines   On

[FILTER]
    Name                kubernetes
    Match               kube.*
    Merge_Log           On
    Keep_Log            Off
    K8S-Logging.Parser  On
    # → 產生 record 裡的 kubernetes 物件（namespace_name / pod_name / labels ...）

[OUTPUT]
    Name                cloudwatch_logs
    Match               kube.*
    region              us-west-2
    # 一 app 一 namespace → 用 namespace 當切分 key
    log_group_template  /eks/$kubernetes['namespace_name']
    log_group_name      /eks/fallback        # template 取不到 key 時的退路
    log_stream_template $kubernetes['pod_name'].$kubernetes['container_name']
    auto_create_group   On
    log_retention_days  30`}</code></pre>
        <p>
          結果：每個 namespace（= 一個 app）一個 log group（如 <code>/eks/checkout-api</code>），
          stream 細到 pod + container，排查時直接鎖定服務。
          （若哪天某個 namespace 裡塞了多個 app 想再細分，再把 template 接上 label，例如
          <code>…/$kubernetes['labels']['app']</code>。）
        </p>

        <h2>4. 切分時要注意的點</h2>
        <ul>
          <li><strong>template 一定要配 <code>log_group_name</code> fallback</strong>：template 取不到 key 時會落到 <code>log_group_name</code> 指定的退路 group，而不是整批失敗。用 <code>namespace_name</code> 當 key 幾乎一定有值（fallback 主要接住非預期 / 非 kube 的 record）；但若改用 label 切，沒貼該 label 的 pod 就會全進 fallback，更要設好。</li>
          <li><strong>為什麼用 namespace 而不是 label</strong>：<code>namespace_name</code> 是 metadata 一定帶的欄位，不依賴各 team 有沒有乖乖貼 label，最穩。只有當「一個 namespace 裡有多個 app」需要再細分時，才需要退而求其次依賴一個一致的 label（例如 <code>app.kubernetes.io/name</code>）。</li>
          <li><strong>group 數量別爆量</strong>：以 namespace / app 切是合理粒度；別用 pod name 之類高基數的東西當 group（那是 stream 的工作）。group 太多會難管理、也會撞到帳號層級的 CloudWatch 限制。</li>
          <li><strong>plugin 版本</strong>：舊的 <code>cloudwatch</code>（C 版）外掛不支援 template；要動態切 group 要用 <code>cloudwatch_logs</code>（Go 版，aws-for-fluent-bit 內建）。</li>
        </ul>

        <h2>5. 優化一：log group retention，防止費用爆炸</h2>
        <p>
          CloudWatch log group 的 retention 預設是<strong>「永不過期」(Never expire)</strong>，log 會一直堆著，
          storage 成本只增不減。切分成 per-app group 之後，順手把每個 group 的 retention 設起來。
        </p>
        <p>
          先理解計費：CloudWatch Logs 收兩塊錢 —— <strong>ingestion</strong>（每 GB 收進來）和
          <strong>storage</strong>（每 GB-月）。retention 砍的是 <strong>storage</strong> 那塊（過期資料自動刪），
          <strong>不影響 ingestion</strong>。所以 retention 控的是「存量成本」；要降「進量成本」得從減少 log 量（過濾、降噪）下手，那是另一條路。
        </p>
        <p>兩種設法：</p>
        <ul>
          <li><strong>Fluent Bit <code>log_retention_days</code></strong>：只在「該 group 被自動建立的當下」套用一次，對<strong>已存在</strong>的 group 無效 —— 切分前就存在的舊 group 不會被它追溯設定。</li>
          <li><strong>直接對 group 設 <code>PutRetentionPolicy</code>，或用 IaC 統一管</strong>：例如 Terraform 的 <code>aws_cloudwatch_log_group</code> 帶 <code>retention_in_days</code>，確保每個 per-app group 都有一致的 retention，也避免有人手動新建 group 時漏設。</li>
        </ul>

        <h2>6. 優化二：提升 Fluent Bit 可靠度（filesystem buffer + health endpoint）</h2>
        <h3>6-1. buffer 寫到 filesystem，防止掉 log</h3>
        <p>
          Fluent Bit 預設把 buffer 放<strong>記憶體</strong>（<code>Mem_Buf_Limit</code>）。問題是：pod 掛掉、重啟，
          或記憶體塞滿時，還沒送出去的 in-flight log 就<strong>直接消失</strong>。對排查來說，最該留的往往就是出事那一刻的 log。
        </p>
        <p>
          解法是開 <strong>filesystem buffering</strong>：把 buffer chunk 落地到磁碟。Fluent Bit 重啟後，
          磁碟上的 chunk 還在，會被重新載入、續送 —— 達成 <strong>at-least-once</strong>，不會因為 collector 自己重啟就掉 log。
        </p>
        <pre><code>{`[SERVICE]
    storage.path              /var/log/flb-storage/
    storage.sync              normal
    storage.checksum          off
    storage.max_chunks_up     128
    storage.backlog.mem_limit 64M

[INPUT]
    Name          tail
    Tag           kube.*
    Path          /var/log/containers/*.log
    storage.type  filesystem        # ← 這個 input 的 buffer 落地到磁碟
    Mem_Buf_Limit 50MB`}</code></pre>
        <ul>
          <li><strong><code>storage.path</code> 要有實體 volume</strong>：DaemonSet 用 <code>hostPath</code> 才能在 pod 被重建後仍保留磁碟上的 backlog；<code>emptyDir</code> 只擋得住 container 重啟，pod 被刪就沒了。</li>
          <li><strong><code>storage.backlog.mem_limit</code></strong>：控制重啟後一次從磁碟 backlog 載回記憶體的量，避免一口氣灌爆記憶體。</li>
          <li><strong>代價</strong>：多了磁碟 I/O 與一點延遲，換來「collector 重啟不掉 log」。搭配 output 本身的 retry，CloudWatch 短暫不可用時也能撐住。</li>
        </ul>

        <h3>6-2. 開 HTTP health endpoint，讓 k8s 能自動重啟壞掉的 collector</h3>
        <p>
          filesystem buffer 解決「重啟後不掉 log」，但還缺一塊：<strong>collector 卡死了誰來重啟它</strong>？
          Fluent Bit 內建一個 HTTP server，開起來後會暴露 <code>/api/v1/health</code>，可以直接接到 k8s 的 liveness / readiness probe，
          collector 不健康時就讓 k8s 把它打掉重來。
        </p>
        <pre><code>{`[SERVICE]
    HTTP_Server   On
    HTTP_Listen   0.0.0.0
    HTTP_Port     2020
    Health_Check  On          # 啟用 /api/v1/health 的健康判斷
    HC_Errors_Count       5   # 視窗內錯誤超過此數 → unhealthy
    HC_Retry_Failure_Count 5  # 視窗內重試失敗超過此數 → unhealthy
    HC_Period             60`}</code></pre>
        <pre><code>{`# DaemonSet 的 probe 指到健康端點
livenessProbe:
  httpGet:
    path: /api/v1/health
    port: 2020
  initialDelaySeconds: 10
  periodSeconds: 10`}</code></pre>
        <p>
          <strong>注意</strong>：<code>Health_Check On</code> 時 <code>/api/v1/health</code> 的健康與否是看
          <code>HC_Errors_Count</code> / <code>HC_Retry_Failure_Count</code> 在 <code>HC_Period</code> 視窗內的累計；
          沒開這個的話該端點只反映 process 活著、不反映「送 log 一直失敗」。兩個 HC 門檻要按實際流量調，太敏感會頻繁重啟。
        </p>

        <h2>7. 一句話總結</h2>
        <p>
          「串接 CloudWatch」= <code>cloudwatch_logs</code> output 用 IRSA 身份把 log 寫進某個 group；
          「按 app 切分」= 把寫死的 <code>log_group_name</code> 換成吃 Kubernetes metadata 的 <code>log_group_template</code>（一 app 一 namespace，所以實作用 <code>namespace_name</code> 當 key）。
          中間那個 <strong>kubernetes filter</strong> 是讓這件事成立的關鍵 —— 沒有它，record 裡就沒有 namespace/app 可以拿來切。
          上線後再用 <strong>retention</strong> 壓 storage 成本、用 <strong>filesystem buffer</strong> 守住 collector 重啟時的 log。
        </p>
      </div>

      <a class="back-link" href="/notes/">← 回筆記列表</a>
    </div>
  </article>

  <footer>
    <div class="container">
      © 2026 {profile.name}. Built with Astro · 部署於 GitHub Pages
    </div>
  </footer>
</Layout>
