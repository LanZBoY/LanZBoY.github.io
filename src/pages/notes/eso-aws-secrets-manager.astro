---
import Layout from '../../layouts/Layout.astro';
import { notes, profile } from '../../data/site';

const meta = notes.find((n) => n.slug === 'eso-aws-secrets-manager')!;
---

<Layout title={`${meta.title} · ${profile.name}`}>
  <nav class="nav">
    <div class="container">
      <a href="/" class="logo">{profile.name}<span>.dev</span></a>
      <div class="links">
        <a href="/#about">關於</a>
        <a href="/#experience">經歷</a>
        <a href="/#projects">作品</a>
        <a href="/#awards">殊榮</a>
        <a href="/notes/" class="active">筆記</a>
        <a href="/#contact">聯絡</a>
      </div>
    </div>
  </nav>

  <article class="note">
    <div class="container">
      <a class="back-link" href="/notes/">← 回筆記列表</a>
      <p class="note-date">{meta.date}</p>
      <h1>{meta.title}</h1>
      <div class="tags">{meta.tags.map((t) => <span>{t}</span>)}</div>

      <div class="prose">
        <p>
          記錄用 External Secrets Operator (ESO) 把 AWS Secrets Manager 的 secret 同步進 EKS、
          並接上 Stakater Reloader 自動套用的設計與踩坑。文中的帳號 ID、app 名稱皆為示意值。
        </p>

        <h2>TL;DR：完整自動鏈</h2>
        <div class="flow">
          <div class="flow-node flow-accent"><strong>AWS Secrets Manager</strong><small>PutSecretValue</small></div>
          <div class="flow-arrow"><span class="flow-label">ESO poll（≤ refreshInterval）</span><span class="flow-tip">值真的變才更新</span></div>
          <div class="flow-node"><strong>k8s Secret</strong><small>dataFrom.extract 攤平成各 key</small></div>
          <div class="flow-arrow"><span class="flow-label">reloader 偵測 Secret hash 變</span></div>
          <div class="flow-node"><strong>Rolling restart workload</strong></div>
          <div class="flow-arrow"><span class="flow-label">新 Pod 重新讀取</span></div>
          <div class="flow-node flow-accent"><strong>新 Pod envFrom 吃到新值</strong><small>全程無人工</small></div>
        </div>
        <p>
          <strong>三段各自獨立</strong>：ESO 只負責「AWS → k8s Secret」；「k8s Secret → 跑著的 pod」要 reloader 補。
          ESO 不會讓 pod 重讀。
        </p>

        <h2>架構與授權邊界</h2>
        <pre><code>{`ExternalSecret (任意 ns) ──ref──► ClusterSecretStore "shared-secrets"
                                         │ auth
                                         ▼
                          IRSA shared-secrets-reader  ──read──►  AWS SM: <env>-shared-*
                          (只能讀 <env>-shared-*)

myapp ns:
ExternalSecret ──ref──► SecretStore "myapp-secret-store"
                              │ auth
                              ▼
                          IRSA myapp  ──read──►  AWS SM: myapp 私密 (rds!db-* / api-key)`}</code></pre>
        <p>
          重點：能讀什麼由 <strong>IRSA 身份的 policy</strong> 決定。<code>shared-secrets-reader</code> 只開
          <code>&lt;env&gt;-shared-*</code>，所以就算任意 ns 的 ExternalSecret ref 了 ClusterSecretStore，
          也撈不到 app 私密（那條走 app 自己的 SecretStore + 專屬 IRSA）。
        </p>

        <h2>1. ESO 核心觀念：SecretStore vs ExternalSecret</h2>
        <table>
          <thead><tr><th></th><th>SecretStore / ClusterSecretStore</th><th>ExternalSecret</th></tr></thead>
          <tbody>
            <tr><td>職責</td><td>怎麼連後端、用<strong>誰的身份</strong>連</td><td>抓哪顆 secret、哪些 key、產出哪個 k8s Secret</td></tr>
            <tr><td>內容</td><td>provider=AWS SM、region、auth（IRSA SA）</td><td>secretStoreRef、dataFrom / data[].remoteRef、target</td></tr>
            <tr><td>數量</td><td>= <strong>認證身份數</strong>（不是 secret 數）</td><td>每顆要同步的 secret 一個</td></tr>
          </tbody>
        </table>
        <p>
          一對多關係：一個 store 底下可掛 N 個 ExternalSecret。<strong>新建 SecretStore 的時機 = 要換讀取身份時</strong>，
          不是每顆 secret 建一個。
        </p>

        <h2>2. 授權邊界 = 身份的 IAM policy（關鍵觀念）</h2>
        <p>
          ClusterSecretStore / SecretStore <strong>本身沒有「secret 範圍」欄位</strong>。能讀到什麼，
          完全由它認證的那個 <strong>IRSA 身份的 policy</strong> 決定 —— 不是 store、也不是 ExternalSecret。
        </p>
        <p>
          所以 ClusterSecretStore 若綁「過寬身份」（例如 ESO controller），任何 namespace 寫個 ExternalSecret ref 它，
          就能讀到該身份能讀的<strong>所有</strong> secret（含別人的）。ExternalSecret 寫在哪個 ns 不構成限制。
        </p>

        <h2>3. Secret 分層 & 命名慣例</h2>
        <p>
          真正重要的不是「叫什麼名字」，而是<strong>有一套一致、可被 IAM wildcard 涵蓋的命名規則</strong>
          —— 自訂或直接遵循公司既有規範都可以，能讓 policy 用前綴一刀切即可。
          下表只是一種範例 scheme，把 secret 依<strong>機密性</strong>分成「環境共用」與「app 私密」兩層：
        </p>
        <table>
          <thead><tr><th>層</th><th>範例命名</th><th>用哪個 store</th><th>身份</th></tr></thead>
          <tbody>
            <tr><td>env 共用（低敏感，如 AI API key）</td><td><code>&lt;env&gt;-shared-*</code></td><td>ClusterSecretStore</td><td>專屬 scoped reader（只讀共用前綴）</td></tr>
            <tr><td>app 私密（db 密碼 / per-tenant key）</td><td><code>&lt;env&gt;-&lt;svc&gt;-*</code></td><td>per-namespace SecretStore</td><td>app 自己的 SA + IRSA</td></tr>
          </tbody>
        </table>
        <p>不管採哪種命名，下面幾個原則都成立：</p>
        <ul>
          <li><strong>按機密性分流，不是按「現在 vs 未來」</strong>。私密 secret 一旦能被 shared store 讀到，暴露窗口當下就發生，事後拆補不回。</li>
          <li>私密層後補完全可以，但「建 policy」不能晚於「該私密 secret 開始存在」—— 兩件事要綁一起做。</li>
          <li>policy 是 <strong>per-app 不是 per-secret</strong>：一個 app 10 顆 secret 仍是一條 policy（一個前綴 wildcard 全包）。只吃 shared 的 app 零自有 policy。</li>
          <li>若命名用 wildcard 收尾，前面留個 delimiter（如 <code>dev-myapp-*</code> 不是 <code>dev-myapp*</code>），避免 svc 名互為前綴誤中。</li>
        </ul>

        <h2>4. JSON 多 key secret + dataFrom.extract</h2>
        <p>一顆 secret 存一包 JSON（多 key），ESO 用 <code>dataFrom.extract</code> 自動攤平成 k8s Secret 的各別 key：</p>
        <pre><code>{`dev-shared-ai-api-keys  →  {"openai":"sk-x","anthropic":"sk-y"}

spec:
  dataFrom:
    - extract:
        key: dev-shared-ai-api-keys
# 結果 k8s Secret 自動有 openai / anthropic 兩個 key`}</code></pre>
        <p>只想挑其中一個 key 用 <code>data[].remoteRef.property</code>；整包帶用 <code>dataFrom.extract</code>。</p>

        <h2>5. 變更偵測：ESO 是輪詢不是 push</h2>
        <ul>
          <li>ESO 按 <code>refreshInterval</code> 主動 poll AWS，抓回比對，<strong>值真的變</strong>才更新 k8s Secret（同值 put 不會動）。</li>
          <li><code>refreshInterval: 0</code> = 只同步一次不 poll。</li>
          <li>k8s Secret → pod：<code>env</code>/<code>envFrom</code> 完全不會自動變（要重啟）；volume mount 檔案會延遲同步但 process 不會自己重讀。</li>
          <li>強制立即同步：<code>kubectl annotate externalsecret X force-sync=$(date +%s) --overwrite</code></li>
        </ul>

        <h2>6. reloader：方法 A vs B</h2>
        <p>
          Stakater Reloader 監看 k8s 物件 hash，<strong>不在乎來源</strong>（ESO / kubectl / helm / ConfigMap 一視同仁），
          被引用的物件變了就 rolling restart。Opt-in annotation：
        </p>
        <table>
          <thead><tr><th></th><th>方法 A <code>reloader.stakater.com/auto: "true"</code></th><th>方法 B <code>secret.reloader.stakater.com/reload: "name"</code></th></tr></thead>
          <tbody>
            <tr><td>盯什麼</td><td>workload 引用到的<strong>所有</strong> Secret / ConfigMap</td><td>只盯指定名稱</td></tr>
            <tr><td>好處</td><td>零維護、ConfigMap 也吃</td><td>精準可控</td></tr>
            <tr><td>風險</td><td>引用到高頻變動 / 共用 CM 會收到不想要的重啟</td><td>引用變了要手動維護清單</td></tr>
          </tbody>
        </table>
        <p>A 只盯<strong>真的被引用</strong>（env / envFrom / volume）的物件；沒被 mount 就不管。別同時貼 A 跟 B。</p>
        <p>
          <strong>用 ConfigMap 觸發 reloader：把 config 從 Helm 拆出來。</strong>reloader 同樣會盯 ConfigMap（不只 Secret）。
          若某份 config 的變動頻率跟 app deploy 週期不同、而且改它只需更新 ConfigMap（不動 image / chart），
          可考慮把這份 ConfigMap 從 Helm chart 拆出來單獨管理（如 IaC 或獨立 pipeline）：改 config → 只 apply 那份 ConfigMap →
          reloader 偵測 → rolling restart → 新 config 生效，不必跑整包 <code>helm upgrade</code>，把
          <strong>config 生命週期跟 app deploy 生命週期解耦</strong>。權衡：拆出來後 config 與 chart 不在同一處，
          chart 預期的 key 改名時別漏改外部 ConfigMap。適合「config 穩定結構、只是值會獨立調」的情境。
        </p>

        <h2>7. IAM ARN 細節</h2>
        <ul>
          <li><strong>Secrets Manager ARN 有 6 碼隨機尾碼</strong>（<code>name-AbCdEf</code>），建立前不可知 → IAM Resource <strong>一定要 wildcard 收尾</strong>。即使用 bare name 呼叫，IAM 比對的還是含尾碼的完整 ARN。</li>
          <li><code>region=*</code> <strong>真的會</strong>跨 region 讀（同帳號跨 region 不需 resource policy）→ 該 pin。</li>
          <li><code>account=*</code> <strong>不會</strong>跨帳號讀（跨帳號要對方 secret 的 resource policy 也放行）→ pin 是 hygiene 不是硬邊界，但 least-privilege 仍建議 pin。</li>
          <li>用 IaC 組出完整 ARN，例如 <code>arn:aws:secretsmanager:us-west-2:&lt;account-id&gt;:secret:dev-shared-*</code>（帳號由 <code>get_aws_account_id()</code> 帶入、region 由變數帶入）。</li>
        </ul>

        <h2>8. Config 放哪（分流）</h2>
        <table>
          <thead><tr><th>性質</th><th>放哪</th></tr></thead>
          <tbody>
            <tr><td>敏感（key / 密碼 / token）</td><td>Secrets Manager <code>&lt;env&gt;-shared-*</code> + ESO</td></tr>
            <tr><td>單 workload 專屬、靜態</td><td>inline Deployment <code>env</code></td></tr>
            <tr><td>多 workload 共用、靜態 / 想跟 deploy 解耦</td><td>獨立 ConfigMap（從 Helm 拆出）+ reloader，見第 6 段</td></tr>
            <tr><td>runtime 要可變</td><td>SSM Parameter Store</td></tr>
          </tbody>
        </table>
        <p>
          <strong>ESO 的 target 永遠是 k8s Secret，不會產 ConfigMap</strong>。所以「config 走 ESO」最後也是落在 Secret 物件（值非敏感而已）。
          env var 不管 inline 還是來自 ConfigMap，改值都要重啟 pod。遷移邊界：現在用 CI variable 注入的東西，
          <strong>敏感的</strong>搬去 SM + ESO（pipeline 不再碰 secret 值），<strong>非敏感 config</strong> 留著繼續 var 注入。
        </p>

        <h2>9. 操作 gotchas</h2>
        <ul>
          <li><code>kubectl logs deploy/X</code>（含 <code>-f</code>）<strong>綁定單一 pod instance</strong>，rolling restart 後該 pod 一死 stream 就斷，不會接新 pod。跨重啟連續看用 <code>stern X -n ns</code>。</li>
          <li>消費端 pod 若比 secret 早起，會 Error / <code>CreateContainerConfigError</code>；secret 同步出來後重滾即正常。</li>
          <li>k8s Secret 的 <code>.data</code> 是 base64，<code>get -o yaml</code> 看到的要 decode 才是明文。</li>
        </ul>

        <h2>10. 這次建立的東西</h2>
        <p>用 Terraform / Terragrunt 落地的元件（依職責拆開，方便重用與檢視最小權限）：</p>
        <ul>
          <li>一個 <strong>module</strong>：建 scoped SA + ClusterSecretStore。</li>
          <li>一條 <strong>IAM policy</strong>：只開 <code>&lt;env&gt;-shared-*</code>（pin region + account）。</li>
          <li>一個 <strong>IRSA role</strong>：trust 綁 <code>external-secrets:shared-secrets-reader</code> 這個 SA。</li>
          <li>一組 <strong>SA + ClusterSecretStore</strong>（<code>shared-secrets</code>）。</li>
        </ul>
        <p>
          follow-up：收斂 ESO controller policy 上那幾條 app 專屬的 SM read 權限（dormant、且與 app 自己的 SecretStore 重複），
          並在各環境一併檢查。
        </p>
      </div>

      <a class="back-link" href="/notes/">← 回筆記列表</a>
    </div>
  </article>

  <footer>
    <div class="container">
      © 2026 {profile.name}. Built with Astro · 部署於 GitHub Pages
    </div>
  </footer>
</Layout>
